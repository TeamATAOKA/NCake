<div class="container">
  <p id="notice"></p>
  <div class="row">
  </div>
  <h1>ジャンル追加・一覧画面</h1>

  <%= form_for(@genre) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :name, class: "col-xs-12" %>

    <%= form_with(model: genre, local: true) do |f| %>
      <%= f.label :genre_status, :有効 %>
      <%= f.radio_button :genre_status, 1 %>
      <%= f.label :genre_status, :無効 %>
      <%= f.radio_button :genre_status, 2 %>
      <%= f.submit '追加', class: "col-xs-12 btn btn-primary" %>
    <% end %>
  <% end %>

		<table class="table table-striped">
			<thead>
				<tr>
					<th>ジャンル</th>
					<th>状態</th>
					<th></th>
				</tr>
			</thead>
            <tbody>
            <% @genres.each do |book| %>
 	            <tr>
                   <td><%= genre.name %></td>
                   <td><%= genre.genre_status %></td>
                   <td><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "col-xs-2 btn btn-info" %></td>
 	            </tr>
            <% end %>
            </tbody>
        </table>
</div>