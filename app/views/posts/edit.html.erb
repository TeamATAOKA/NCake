<div class="container">
  <div class="row">
    <h2>配送先編集</h2>
    <%= form_for @post, url: user_post_path(@user) do |f| %>
    <table class="table">
      <tr>
    	<th><%= f.label :postcode, "郵便番号(ハイフンなし)" %></th>
    	<th><%= f.text_field :postcode, class: "col-xs-12" %></th>
      </tr>
      <tr>
        <th><%= f.label :address, "住所" %></th>
        <th><%= f.text_field :address,class: "col-xs-12" %></th>
      </tr>
      <tr>
        <th><%= f.label :name, "宛名" %></th>
        <th><%= f.text_field :name, class: "col-xs-12" %></th>
      </tr>
    </table>
            <%= f.submit "変更を保存", class: "btn btn-primary col-xs-4 pull-right" %>
    <% end %>
  </div>
</div>